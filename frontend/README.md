# Proteus frontend

React, TypeScript, Vite, and Tailwind interface for the Proteus
professor-calibrated grading application.

From the repository root, use `make setup`, `make seed`, and `make dev`.
Frontend-only commands are available through `npm run dev`, `npm run build`,
and `npm run lint`.
